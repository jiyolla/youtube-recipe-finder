[project]
name = "youtube-recipe-finder"
version = "0.1.0"
description = "Find optimal recipe combinations that minimize your shopping list"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "youtube-transcript-api>=1.2.3",
]
